{
  "name": "raga-ai-healthcare",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start:mfe": "npm run dev --workspace=apps/host",
    "build": "npm run build --workspaces"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0"
  }
}
